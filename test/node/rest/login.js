/*global describe, it */
// var login = require("../../../lib/rest/login");

describe("login", function () {
  it("should send a 400 if the username/password does not match");
  it("should send a 500 if there is an exception");
  it("should send a 200 if the user can login");
});