/*global describe, it */
define(
  // ["anax"], 
  function (/*Anax*/) {
    describe("anax", function () {
      it("should have a default view state");
      it("should fetch a user model to AppView");
      it("should do something when it can't get that user model - say it is not logged in or can't connect to server?");
    });
  }
);